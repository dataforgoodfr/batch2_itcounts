# D4G - itcounts

Steps for successful execution:<br/>
1. Install MongoDB <br/>
3. Install Nodejs and NPM<br/>
4. Navigate to the directory using command prompt and run npm install, this will install the dependencies<br/>
5. Navigate to the directory using command prompt and run npm start<br/>
6. In your browser go to localhost:8080/index.html<br/>
